{
  "name": "parcel-types-issue",
  "version": "1.0.0",
  "description": "Type declaration file in build output is not valid",
  "main": "dist/index.js",
  "types": "dist/types.d.ts",
  "scripts": {
    "one": "rm -rf .parcel-cache dist && parcel build one/main.ts",
    "two": "rm -rf .parcel-cache dist && parcel build two/main.ts"
  },
  "devDependencies": {
    "@parcel/packager-ts": "2.3.2",
    "@parcel/transformer-typescript-types": "2.3.2",
    "parcel": "2.3.2",
    "typescript": "4.6.2"
  }
}
